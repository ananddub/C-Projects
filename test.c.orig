#include <conio.h>
#include <stdarg.h>
#include <stdio.h>
int test() {
  char ch;
  int flag = 1;
  while (1) {

    while (kbhit()) {
      if (flag)
        clrscr();
      ch = getch();
      printf("{char(ch)=[%c},{int(ch)=[%d]}\n", ch, ch);
      flag = 0;
    }
    flag = 1;
  }
}
int ptr(int, ...);
int main() {
  char data[] = "hello world ";
  test();
  clrscr();
}
void prt(int count, ...) {}
}
