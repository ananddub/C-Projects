#include <krishna.h>int main() {        char src[] = "Macbook Utra pro";        char dest[200];        int len=10;        // strcpy(dest,src);         // tmpnam(dest);        printf("\nLength: %g  \n",len);        return 0;}